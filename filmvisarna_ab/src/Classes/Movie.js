export default class Movie{

    constructor(title, image, genre, movieLength, language, description, trailer){
        this.title = title,
        this.image = image,
        this.genre = genre,
        this.movieLength = movieLength,
        this.language = language,
        this.description = description,
        this.trailer = trailer
    }
}
